#!/usr/bin/env node
var APM = require('../'),
    rc = require('rc'),
    apm;

var config = rc('apm', {
  prefix: process.env.HOME || process.env.USERPROFILE
});

apm = new APM(config);

if (process.argv[2] === 'install' && process.argv[3]) {
  apm.install(process.argv[3]);
}
